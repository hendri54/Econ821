% Startup routine for Deterministic 2 Period OLG Model
% ----------------------------------------------------

fprintf('Startup: 2 period olg model \n');

% Switch to base directory for model
cS = const_821;
cd(fullfile(cS.olg2dDir, 'progs'));

% eof